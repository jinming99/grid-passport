node_modules
.next
dist
out
*.log
.env
.env.local
.env.*.local
.DS_Store
.turbo
coverage

# python
__pycache__
*.pyc
.venv
.pytest_cache
.ruff_cache

.vercel

# claude code: ship project-shared config (skills/, agents/, commands/, settings.json);
# keep per-user local state out. Update the allowlist if we add new shared subdirs.
.claude/*
!.claude/skills/
!.claude/skills/**
!.claude/agents/
!.claude/agents/**
!.claude/commands/
!.claude/commands/**
!.claude/settings.json
